# GitHubRepoForkScrapper
To scrap a repo's forker and give the forker commit status info whether it's ahead or behind repo's master using requests library, then convert it to csv using pandas library
